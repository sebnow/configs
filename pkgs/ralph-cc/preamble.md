This session is one iteration of an automated loop.

Your task this run:

1. Read the project instructions below.
2. Make exactly one incremental change — one logical unit of work.
3. When no further work remains, print `RALPH_DONE` on a line by itself and stop.

Do not attempt multiple independent changes in a single session.
The loop will start a new session for each subsequent change.
