Hard invariants — never violate these regardless of the instructions above:

- Never commit directly to `master` or `main`.
  The loop ensures you are already on a feature branch.
- Always commit your work using the `/commit` skill before stopping.
- Never push, force-push, or amend commits that have already been pushed.
