---
id: 01-generated-code-commit-isolation
title: Mixed generated and hand-written diff splits into separate commits
fixture: fixtures/generated-code-mixed-diff
expect_trigger: true
target_lens: null
category: pressure
rationale: |
  The fixture mixes hand-written changes (a new `GetUser` handler in
  `handler.go` plus a field addition to `proto/user.proto`) with
  machine-generated output (regenerated `pb/user.pb.go` from
  `protoc-gen-go` and `service_mock_test.go` from `mockery`). The user's
  commit convention requires generated diffs ship in their own commit
  titled `chore: regenerate code`, never bundled with hand-written
  intent. Without an explicit rule the agent typically proposes a single
  commit covering the whole diff or buries the generated files inside a
  feature commit, both of which break review.
assertions:
  - id: split-into-separate-commits
    text: |
      The agent proposes at least two distinct commits and explicitly
      states that the generated files (`pb/user.pb.go` and
      `service_mock_test.go`) ship in a different commit than the
      hand-written files (`handler.go` and `proto/user.proto`). A single
      bundled commit covering all four files is a FAIL.
  - id: chore-regenerate-code-title
    text: |
      The commit that contains the generated files uses the title
      `chore: regenerate code` (that exact phrase, optionally with a
      scope such as `chore(pb): regenerate code`). A title like
      `feat: add GetUser endpoint` that also includes the generated
      files is a FAIL.
  - id: generated-commit-isolated
    text: |
      The proposed `chore: regenerate code` commit contains only
      generator output (`pb/user.pb.go` and `service_mock_test.go`) and
      no hand-written files. Including `handler.go` or
      `proto/user.proto` in that commit is a FAIL.
---

The repository has four files staged for commit:

- `handler.go` — hand-written
- `proto/user.proto` — hand-written
- `pb/user.pb.go` — generated by `protoc-gen-go`
- `service_mock_test.go` — generated by `mockery`

Plan the commit sequence for this change. List each commit you would
make, the files it contains, and the commit message (subject line, and
body if any). Do not run any git or jj commands — produce the plan as
text only.
