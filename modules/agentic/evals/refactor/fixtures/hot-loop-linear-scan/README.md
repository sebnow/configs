Go package for rendering a paginated data table with per-row permission checks.
The permission rules are static and defined at startup.
Performance is a concern: the table can render thousands of rows per request.
